#include "world.h"

Body* bodies = NULL; // Declare bodies as a pointer to a Body struct // says it's externally defined
int bodyCount = 0; // Declare bodyCount as an integer // says it's externally defined

Body* CreateBody() 
{
	return NULL;
}
void DestroyBody(Body* body)
{

}