#pragma once

//#include "raylib.h"
//#include "raymath.h"
//
//extern Vector2 position;
//extern float outerRadius;
//extern float innerRadius;
//extern int numPoints;
//extern Color color;
//extern float scaleFactor;
//extern float scaledOuterRadius;
//extern float scaledInnerRadius;
//
//void DrawStar(Vector2 position, float outerRadius, float innerRadius, int numPoints, Color color, float randomTwinkleOffset, float scaleFactor);
//void DrawTradStar(Vector2 position, float outerRadius, float innerRadius, int numPoints, Color color, float randomTwinkleOffset);
//void DrawWeirdStar(Vector2 position, float outerRadius, float innerRadius, int numPoints, Color color, float randomTwinkleOffset);
//void DrawStarRound(Vector2 position, float outerRadius, float innerRadius, int numPoints, Color color, float randomTwinkleOffset);
//void ShapeWTF(Vector2 position, float outerRadius, float innerRadius, int numPoints, Color color);
//
//Color GetStarColor(int temperature);
//Color ApplyDimming(Color color, float factor);